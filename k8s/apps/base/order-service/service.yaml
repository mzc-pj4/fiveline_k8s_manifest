apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: fiveline
  labels:
    app: order-service
spec:
  type: ClusterIP
  selector:
    app: order-service
  ports:
    - name: http
      port: 8003
      targetPort: 8003
      protocol: TCP
