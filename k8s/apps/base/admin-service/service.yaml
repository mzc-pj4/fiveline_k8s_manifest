apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: fiveline
  labels:
    app: admin-service
spec:
  type: ClusterIP
  selector:
    app: admin-service
  ports:
    - name: http
      port: 8004
      targetPort: 8004
      protocol: TCP
